dependencies {
    api project(':client:java-armeria')
    api(project(':server')) {
        exclude group: 'org.apache.thrift', module: 'libthrift'
        exclude group: 'com.linecorp.armeria', module: 'armeria-thrift0.9'
    }
}
